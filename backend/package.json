{
  "name": "inspirations-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.js",
    "seed": "node prisma/seed.js",
    "build": "cd ../frontend && npm install --include=dev && npm run build",
    "start": "node src/index.js",
    "postinstall": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^8.0.4",
    "prisma": "^5.10.0",
    "resend": "^6.9.4"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
